<%- include('partials/header') %>
    <main>
        <h1>Dashboard</h1>
        <p><strong>Name:</strong>
            <%= name %>
        </p>
        <p><strong>Email:</strong>
            <%= email %>
        </p>

        <% if (user && user.role_name==='admin' ) { %>
            <a href="/users" class="org-add-link">View All Users</a>
            <% } %>
    </main>
    <%- include('partials/footer') %>